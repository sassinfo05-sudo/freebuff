# Test Credentials — Zanelvo Dev Studio

Single-user app. One admin password gates the whole app (env `ADMIN_PASSWORD`). No email/username.

- **Admin password**: `zanelvo-admin-2026`

Login at the app root (Login page) using this password.
